### Locked Nodes

The following nodes were locked by another user during the noop run,
consequently they are unrepresented in the noop output.
{{ range $hosts, $lockmsg := .Locked }}
- *Hosts:* `{{ $hosts }}`
  -  *Lock Reason:* **{{ $lockmsg }}**
{{ end }}
