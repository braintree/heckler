### Puppet Resource Changes (noop)
{{- if eq (len .GroupedResources) 0 }}

No resource changes generated by this commit
{{- else }}
{{ range $resource := .GroupedResources }}
-  **{{ $resource.Title }}**
    {{- if $resource.Diff }}
    - *Diff:*
      ```diff
{{ indent 6 $resource.Diff }}
      ```
    {{- end }}
    - *Hosts:* `{{ $resource.CompressedNodes }}`
    {{- if $resource.DefineType }}
    - *Define Type:* `{{ $resource.DefineType }}`
    {{- end }}
    {{- if $resource.Line }}
    - *Source File:* [{{ $resource.File }}](https://{{ $.Conf.GitHubDomain }}/{{ $.Conf.RepoOwner }}/{{ $.Conf.Repo }}/blob/{{ $.Commit.AsObject.Id }}/{{ $resource.File }}#L{{ $resource.Line }})
    {{- end }}
    {{- range $resource.Events }}
    - *Current State:* `{{ .PreviousValue }}`
    - *Desired State:* `{{ .DesiredValue }}`
    {{- end }}
    {{- if $resource.Logs }}
    - *Logs:*
    {{- range $resource.Logs }}
        - `{{ .Message }}`
    {{- end }}
    {{- end }}
{{ end }}
{{- end }}
